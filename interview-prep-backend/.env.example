# Application Configuration
PORT=7070
APP_ENV=development

# Database Configuration (Path relative to project root or absolute path)
DB_PATH=data/interview_prep.db

# Security & CORS
ALLOWED_ORIGINS=http://localhost:7070,http://127.0.0.1:7070

# HikariCP Connection Pool Settings
DB_POOL_MAX_SIZE=10
DB_POOL_MIN_IDLE=2
